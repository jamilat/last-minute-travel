import { useEffect, useRef } from "react";
import gsap from "gsap";

const TextAnimation = ({ref}) => {
  return (
    <div ref={ref} className="content">
      <h2>So, where you off to today?</h2>
      <h2>So, where you off to today?</h2>
    </div>
  );
};

export default TextAnimation;
